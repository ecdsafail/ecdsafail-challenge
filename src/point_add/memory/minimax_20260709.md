# Minimax Analysis — ecdsa.fail secp256k1 point-addition
**Date**: 2026-07-09 (updated)
**Baseline**: 1,521,518,976 score (T=1,320,762, Q=1,152, CCX=1,397,851, 9024/9024 PASS)
**Verified baseline commit**: `70b2f6e` = `06f6b10` (clean upstream)
**Target**: beat 1,521,518,976

## Baseline Verified
- `06f6b10`: T=1,320,762, Q=1152, 9024/9024 PASS ✓
- `7910e90`: T=1,320,762, Q=1152, 9024/9024 (inconsistent in results.tsv — constprop nondeterminism)
- Constprop removes ~4,728 CCX from the emitted 1,320,762
- `823b4de`: T=1,260,293 but FAIL (someone else's broken submission)

## Top-6 Subroutines (by T×Q contribution)
1. cswap × 516 × 256 bits — **660K T×Q** (DOMINANT, peak-Q driver)
2. mod_square_sub_pm × 1 — **4.5M T×Q** (single call, biggest per-call)
3. mod_add/sub coord × 12 — **3.5M T×Q** (every point-add call)
4. controlled_mod_sub_vented × 258 — **154K T×Q**
5. controlled_mod_add_k × 258 — **124K T×Q**
6. controlled_double × 516 fused — **155K T×Q**

## Binding Constraint
**Q=1,152 is the hard ceiling**. Peak at op 25,841 during `tlm_apply_inverse_mod_sub_register`.
cswap keeps 256(x2) + 256(y2) + 256(tmp) + 256(x_reg) + 124(ancilla) = 1,148 qubits live simultaneously.
**Reducing Q by 256 gives better T×Q win than reducing T at constant Q.**

## Alternative Strategies Evaluated

### Alt 1: Bogan 2-CCX cswap — ❌ BLOCKED (can't find construction)
- Theory: CCZ + 2×CCX using dirty ancilla (conditionally clean)
- Expected: 3→2 CCX per swap, saves ~1,032 CCX total ≈ 3.4%
- Tried: Guessed construction CCZ + CCX + CX + X — **FAIL** (9024 mismatches)
- Paper: Khattar & Gidney, "Rise of Conditionally Clean Ancillae", arXiv:2407.17966 (2025)
- **Need**: Find the exact 2-CCX Fredkin construction from the paper
- **Action**: Extract from PDF / find on QC StackExchange

### Alt 2: cswap width-aware (forward only) — ❌ REJECTED
- Forward pass already uses `current_n = SCHED_J2[i]` — already done
- Verified in gcd.rs:1311 forward cswap: `for j in 0..current_n`
- Reverse cswap CANNOT use width-truncation (binary GCD symmetry requirement)
- Confirmed 2026-07-09: all 4 truncation attempts FAILED correctness

### Alt 3: Pseudo-Mersenne controlled_add+sub — ❌ REJECTED
- H1 attempt: Algorithm 10 for mod_add — correctness FAIL
- Bug: qubit aliasing with cout after zero_and_free
- `arith.rs:1294` panic: cuccaro requires `reg.len() >= lsbs + suf_dirty` where `suf_dirty = n - k - 1`
- cuccaro carry chain is intrinsically O(n) — NOT width-truncatable

### Alt 4: Square-add fusion for peak Q — PENDING
- Theory: Fuse mod_square with the following mod_add to reduce live qubits
- Projected: reduce peak Q by ~256 (from 1,152 to ~896) → 22% score win
- Total emitted T stays same, Q drops
- **Risk**: Fusing打破了the GCD iteration boundary; must preserve correctness

### Alt 5: H3 jump-dialog — PENDING (architecture planned)
- JUMP=3 Triple3 codec: 344 symbols vs 516 (JUMP=2) = −33% symbols
- Problem: Wire-copy Triple3 = 9 bits/sym vs Triple = 7 bits/sym = **net LOSS of 163 bits**
- Wire-copy Triple3 REJECTED — needs real compression (≤6 bits/sym)
- Real win requires Gray-code bijective compression (6 bits → 8 states)
- **Risk**: Gray-code bijectivity hard to prove; forward/reverse must match

### Alt 6: H2 Karatsuba for f=0x1001 — ❌ REJECTED
- Theory: Karatsuba for the f=0x1001 constant in pseudo-Mersenne field
- Reality: GCD uses only addition/subtraction/shift/comparison — NO general multiplication
- `apply_f_times_value` (gcd.rs:1448) = controlled left-shift + conditional XOR — O(n), not mul
- **Karatsuba doesn't apply to GCD arithmetic**

### Alt 7: H3-RNS (Chevignard 2026/280) — PENDING
- Projective coordinates eliminate full modular inverse
- Legendre symbol for sign check (cheap) instead of inverse
- Integration: replace affine GCD output with projective + Legendre
- **Tradeoff**: T increases O(n⁴log²n), Q decreases ~3n²

## Current Status
| Strategy | Status | T Change | Q Change | Score Impact |
|----------|--------|----------|----------|--------------|
| Baseline | ✓ VERIFIED | 1,320,762 | 1,152 | 1,521,518,976 |
| Bogan 2-CCX | ❌ BLOCKED (no construction) | ~−1,032 | 0 | ~−3.4% IF found |
| cswap-width | ❌ REJECTED (already done) | 0 | 0 | 0% |
| PM mod_add | ❌ REJECTED (H1 failed) | N/A | N/A | N/A |
| Square-add fusion | PENDING | 0 | ~−256 | ~+22% IF works |
| H3 Triple3 | PENDING (wire-copy loses) | +? | +? | NEGATIVE unless compression |
| H2 Karatsuba | ❌ REJECTED (N/A) | 0 | 0 | 0% |
| H3-RNS | PENDING | +? | ~−? | TBD |

## Next Steps (Priority Order)
1. **Find Bogan 2-CCX construction** — extract from Khattar-Gidney PDF or QC StackExchange
   - PDF: `/tmp/khattar_gidney.pdf` (already downloaded)
   - Search for "Figure 5" or "2-CCX" in the paper
2. **Square-add fusion** — design the fused mod_square+mod_add circuit
   - Target: reduce peak Q from 1,152 to ~896
3. **H3 Triple3 compression** — find a bijective 6-bit compression for 3-iter Gray codes
   - Must be ≤6 bits/sym to beat Triple (7 bits/sym)

## Key Files
- `gcd.rs:1311`: forward cswap (width-aware, already optimized)
- `gcd.rs:1327`: reverse cswap (full n=256, required by GCD symmetry)
- `mod.rs:74`: `cswap` trait method — replace here for global effect
- `arith.rs:1275`: `controlled_mod_sub_vented` — cuccaro O(n) carry chain
- `square.rs:507`: `mod_square_sub_pm_secp256k1_symmetric` — single 4.5M T×Q call
- `gidney.rs:811`: `controlled_hybrid_add_cout_refs` — peak 1152Q source

## Git History
- `06f6b10`: baseline reset to 70b2f6e clean upstream (VERIFIED PASS)
- `d81e0c0`: codec: Triple3 (inactive at JUMP=2, committed)
- `7910e90`: H1 Algorithm 10 (dead code, reverted)
- `70b2f6e`: revert B iter-0 fusion (previous upstream)
