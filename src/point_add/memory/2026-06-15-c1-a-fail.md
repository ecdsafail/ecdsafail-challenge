# C1 + A attempted, both failed (2026-06-15)

Status: **negative evidence** for next-session session.

## Setup (verified)
- Repo: `/tmp/ecdsafail-run/`
- Baseline reproducer: `target/release/build_circuit` + `eval_circuit` (9024 shots)
- Baseline: **1,432,535.509 avg executed Toffoli, 1170 peak qubits, 0 classical / 0 phase / 0 ancilla garbage → score 1,676,067,120**.
- Submission `408fde2e` (q1170 route, accepted upstream). Source: `results.tsv` last row, `git log` HEAD = "Accept submission 408fde2e".

## Attempt 1 — A: `DIALOG_GCD_SHIFT_BAND_TRIMS` (Lead A from 2026-06-07 measured note)
- **What the note said:** `dialog_gcd_shift_band_trim(step)` at `rounds/dialog/mod.rs:208`; `body` and `body-1` schedules; expected −850 to −1,600 CCX, peak-neutral, island-free.
- **What I found in source:** **Neither function exists.** `grep` for `SHIFT_BAND_TRIMS` in `src/` returns 0 hits. Only the 2026-06-08 markdown mentions it.
- **Verdict:** the note describes a **proposed** knob that was **never implemented in the open-source tree**. The `tools/cuda/island.cu` path (where Lead A may exist) is not in the contestant repo. **No-op for the Rust build.**

## Attempt 2 — C1: `DIALOG_GCD_TOBITVECTOR_CSWAP_BODY_TRIM` 0→1
- **What the note said:** "**value-exact by the body trim's own premise... island-free, nonce 11,201,395,269 survives**" (2026-06-07 measured note Lead C1).
- **What I tried:** flipped 0→1 at `mod.rs:1084` (outer default) and `mod.rs:1423` (tier-3 "safe lock" block).
- **Result on current tree (nonce 55,432,501,896,150 = 408fde2e):**
  - emitted ops: 10,288,412 → **10,283,396** (−5,016 = −0.05% ops, ≈ −1,600 CCX per note's prediction — ΔT estimate was correct).
  - peak qubits: 1170 → 1170 (**peak-neutral ✓**).
  - correctness: **89 classical mismatches** at 9024 shots, 36 phase-garbage batches, 0 ancilla-garbage.
  - **FAILED eval gate.**
- **Conclusion:** the 2026-06-07 note's "island-free" claim was correct for the **q1302 route's nonce 11,201,395,269**. The **current accepted route (q1170, nonce 55,432,501,896,150) is on a different island** where the cswap body trim's realizable-bitlen bound is violated. The trim assumes the swapped high bits are |0> (because the body trim already assumes that for its own sub/add). At nonce 55B, the realizable input set has more inputs that reach those high bits, so the cswap trims away real data.

## Reverted
- All 3 edits reverted. `git diff --stat` clean. `target/release/build_circuit` + `eval_circuit` reproduce baseline 1,432,535.509 / 1170 / 0 mismatches.

## Lesson (durable for next session)
- **2026-06-07 measured note is stale for the current accepted route.** Any "island-free" claim must be **re-verified at the live nonce** (currently 55,432,501,896,150). Lead C1 in particular: **not safe at this nonce**.
- **Lead A env var not wired in source** — must be implemented (in `dialog_gcd_k2_shift_active_width` and a new `dialog_gcd_shift_band_trim` wrapper) before it can do anything.
- **All remaining measured wins (B1, B2) need PROBE_SCRATCH to find a clean borrow lane or binder notch**, then a fresh nonce re-hunt via GPU/CPU GCD prefilter. **CPU-only path is exhausted at this nonce.**

## Next-session handoff prompt
> "Read `src/point_add/memory/2026-06-15-c1-a-fail.md` first. Do NOT retry Lead C1 or Lead A on nonce 55,432,501,896,150 — both fail. If you have GPU access, run PROBE_SCRATCH to find the binder step and the cleanest |0> borrow lane for Lead B2. If not, the structural-rewrite path is closed at the current frontier nonce; pursue either (a) implementing the missing Lead A wiring, or (b) a nonce re-hunt over [0, 1e7) to find one where C1 actually is island-free."
