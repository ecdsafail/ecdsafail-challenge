# 2026-06-02 Apply F_CUT=78 → peak 1543 (T=1,695,087)

Stacks on the ROUND84 x-tail-square shift→doublings peak cut (1567→1543 binder
move). After the square phase drops to ~1542, the global peak is bound by the
two `dialog_gcd_materialized_special_chunked_raw_{sum,difference}` apply phases,
both at 1558 with the prior `DIALOG_GCD_APPLY_CHUNKED_F_CUT=70`.

The chunked apply sub/add is EXACT regardless of F_CUT (full Cuccaro over each
block + an exact `[..F_CUT]` boundary-borrow clear), so F_CUT is value-neutral:
it only rebalances block widths (block 1 = `[F_CUT, 257)`, whose f-load + carry
lane is the peak transient) and reseeds the Fiat-Shamir stream.

- Widening the first cut 70 → 78 narrows block 1 enough to drop the apply phase
  to 1543, the ROUND84 floor. Peak 1558 → 1543.
- F_CUT=78 is the LOWEST cut reaching 1543 on this base (round84 doubles floor
  1542): cut=77 → 1544, cut=78 → 1543. (The frontier's `KARA_SOL_SHIFT22_DOUBLES`
  impl floors at 1543, so it needs cut=79; the `ROUND84_XTAIL_BORROW_CARRIES`
  doubling lanes here floor 1 qubit lower, allowing cut=78 = −798 Toffoli.)
- Cost: +6,384 avg-executed Toffoli (1,688,703 → 1,695,087) for −15 peak, well
  inside the ~1,700 Toffoli/qubit break-even.

Validated island (parallel 2-D `DIALOG_REROLL × DIALOG_POST_SUB_REROLL` search;
clean-island density ~0.4% on this stream, found at fresh reroll 28):

- `ROUND84_XTAIL_BORROW_CARRIES=1`
- `DIALOG_GCD_COMPARE_BITS=59`
- `DIALOG_GCD_APPLY_CHUNKED_F_BLOCKS=2`, `DIALOG_GCD_APPLY_CHUNKED_F_CUT=78`
- `DIALOG_GCD_APPLY_CLEAN_COMPARE_BITS=19`
- `DIALOG_REROLL=28`, `DIALOG_POST_SUB_REROLL=5`
- `./benchmark.sh` → `{score: 2615519241, toffoli: 1695087, qubits: 1543}`,
  all 9024 shots OK (0 classical / 0 phase / 0 ancilla).

Next binder: the round84 x-tail square floor (1542/1543) and the apply phases now
co-bind at 1543; going sub-1543 needs the round84 Solinas `mod_sub_qq` z1_reg
coexistence reduced (hard) or a cheaper apply carry lane (borrowed-carry hosting).
